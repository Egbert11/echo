{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compile c",
            "type": "shell",
            "command": "gcc",
            "args": [
                "-g",
                "${fileDirname}\\${fileBasenameNoExtension}.c",
                "-o", 
                "${fileDirname}\\${fileBasenameNoExtension}.exe"
            ],
            "problemMatcher": [
                "$gcc"
            ]
        }
    ]
}